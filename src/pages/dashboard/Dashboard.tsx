import { BaseLayout } from '../../shared/layouts';

export const Dashboard = () => {
  return (
    <BaseLayout title='Pagina inicial' toolbar={'barra de ferramentasx'}>testando uiui</BaseLayout>
  );
};